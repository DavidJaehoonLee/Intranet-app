import FormControl from "../Modules/formControl.js";

const txtRequested = new FormControl("txtRequested");
const cmbForward = new FormControl("cmbForward");
const txtForwardEmail = new FormControl("txtForwardEmail");

const field = {
    txtRequested,
    cmbForward,
    txtForwardEmail
}

export default field;